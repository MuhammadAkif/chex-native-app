import React from 'react';
import {DVIRInspectionChecklistScreen} from '../../Screens';

const DVIRInspectionChecklistContainer = ({navigation}) => {
  return <DVIRInspectionChecklistScreen navigation={navigation} />;
};

export default DVIRInspectionChecklistContainer;
