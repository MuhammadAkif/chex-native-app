import React from 'react';
import {DVIRVehicleInfoScreen} from '../../Screens';

const DVIRVehicleInfoContainer = ({navigation}) => {
  return <DVIRVehicleInfoScreen navigation={navigation} />;
};

export default DVIRVehicleInfoContainer;
